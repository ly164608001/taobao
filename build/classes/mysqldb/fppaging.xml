<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.lxhrainy.tools.dao.FpPagingMapper"> 
	<select id="getCount" parameterType="Map" resultType="java.lang.Long">
		select count(1) from ${tablename} ${where}
	</select>
	<select id="select" parameterType="Map" resultType="Map">
		select * FROM ${tablename} ${strwhere} ${strorder} limit ${startindex},${pagesize}
	</select> 
	<select id="selectIds" parameterType="Map" resultType="Object">
		select ${keyname} from ${tablename} ${strwhere} ${strorder} limit ${startindex},${pagesize}
	</select> 
</mapper>